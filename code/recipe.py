#Below is the code for a class called recipe which will allow users to create a new recipe and store it in a list??
class Recipe:
    def __init__(self, name):
        #class attributes for Recipe
        self.name = name
        #both ingredients and instructions are gonna need to be store in another data type that allows the holding of
        #multiple items
        #self.ingredients = ingredients
        #self.instructions = instructions
        
    def getIngredients(self):
        #get the ingredients from the user
        return 0